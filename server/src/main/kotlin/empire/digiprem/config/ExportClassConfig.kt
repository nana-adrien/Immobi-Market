package empire.digiprem.config;


import org.springframework.context.annotation.Bean
import org.springframework.context.annotation.Configuration;
import org.springframework.security.crypto.password.PasswordEncoder;

@Configuration
 class ExportClassConfig {
 /*   @Bean
    public fun userAuthenticateService()
      public IUserAuthenticateService userAuthenticateService(PasswordEncoder passwordEncoder,
                                                              UserRepository userRepository,
                                                              UserRoleRepository roleRepository,
                                                              IUserConversationService userConversationService) {
        return new UserDetailServiceImpl(passwordEncoder, userRepository, roleRepository, userConversationService);
    }*/
}
